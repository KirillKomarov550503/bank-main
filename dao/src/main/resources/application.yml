spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bank_main
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: qwerty
    platform: POSTGRESQL
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
    properties:
      hibernate:
        temp:
          use_jdbc_metadata_defaults: false
  application:
    name: bank-main
server:
  port: 8080
